package main

import "fmt"

func main() {
	// var card string = "Ace of Spades"
	// go compiler will figure out the above is the above
	// := says "we have a variable, infer the type based on the variable"
	// we only use := on DEFINITION not reassignment
	card := "Ace of Spades"

	fmt.Println(card)
}
